local Enemy = {}
Enemy.__index = Enemy

function Enemy.create(def, game)
  local enemy = {}
  return enemy
end

function Enemy:update(dt)

end

function Enemy:draw()
  
end

return Enemy
